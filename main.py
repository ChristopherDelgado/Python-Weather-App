# importing GUI as the main graphics portion of the app
# import tkinter as the input for the root of Frame.__init__()

import GUI as graphics
import tkinter as tk

frame = graphics.Frame(tk.Tk())
frame.input()
